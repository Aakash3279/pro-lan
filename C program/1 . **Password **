 // program to enter password by user
 char name[200];
 int uc = 0;
 int lc = 0;
 int num = 0;
 int sym = 0;
 int a = 1;
 while(a < 2){
   int x = 4;
 int i = 0;
 int pw;
 printf("Enter your password : \n");
 scanf("%s", &name);
 pw = strlen(name);
// strlen-- used to find the numbers of letters written by user
printf("\nYour Password length is %d\n",pw);
i=0;
 while(i < pw){
   if(islower(name[i])){
   // to find the lower characters of the password written by user(gyfsuhihi***)
     printf("You have entered a lower letter character!\n");
     x--;
     lc++;
     break; 
   }
  i++;
 }
 i=0;
 while(i < pw){
   if(isupper(name[i])){
    // to find the upper characters of the password written by user(ILJGSAE***)
     printf("You have entered an upper case letter!\n");
     uc++;
     x--;
     break;
   }
   i++;
 }
 i=0;
 while(i < pw){
   if(isdigit(name[i])){
    // to find the numrical characters of the password written by user(12345***)
    printf("You have entered a number!\n");
    num++;
    x--;
    break;
   }
   i++;
 }
i=0;

 while(i < pw){
   if(isalpha(name[i])){}else if(isdigit(name[i])){
   }else{
    // to find the special characters of the password written by user(*&^%$#@)
     printf("You have entered a symbol!\n");
     sym++;
      x--;
     break;
   }
   i++;
 }
 printf("the value is : %d\n",x);

 if(x == 0){
   printf("You password is secured!\n");
 }else{
   printf("Your password is not secured! Please try again!\n");
   a--;
   if(lc == 0){
     printf("Please enter atleast one lower case letter!\n");
   }
   if(uc == 0){
     printf("Please enter atleast one upper case letter!\n");
   }
   if(num == 0){
     printf("Please enter atleast one numerical character!\n");
   }
   if(sym ==0){
     printf("please enter atleast one special character! ($,&,#,@,!)\n");
   }
 }
 a++;
 }
